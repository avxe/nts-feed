# Development and testing dependencies
# Install alongside production deps: pip install -r requirements.txt -r requirements-dev.txt

# Testing
pytest~=8.0
pytest-cov~=5.0

# Linting & formatting
ruff~=0.8.0
